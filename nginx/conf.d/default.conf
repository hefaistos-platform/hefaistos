## Minimal placeholder used by nginx:alpine entrypoint scripts.
## NGINX already includes all files from /etc/nginx/conf.d/*.conf,
## so this file must NOT include hefaistos.conf again.

types_hash_max_size 4096;
